# Expected note outline

This is a structural example for the fictional paper **BiTemporal Language-Guided Change Reasoning for Imaginary Satellite Scenes**. Page numbers and findings are illustrative placeholders, not claims about a real publication.

## 1. 论文身份信息

- 论文 ID：`P900`
- 阅读模式：新手精读
- 证据版本：本地完整 PDF；PDF 印刷页码与阅读器页码的映射已记录
- 代码、数据、DOI：`未找到（虚构示例）`

## 2. 一句话问题与核心结论

通俗结论先行；随后分别标记：`作者主张`、`证据支持`、`分析/推断`。每项均附类似 `[PDF p.3]` 的定位。

## 3. 阅读前置知识

先用直观语言解释双时相影像、视觉—语言对齐和交叉注意力，再给出形式化定义及其与变化检测的关系。

## 4. 方法流程

`时相一影像 + 时相二影像 + 文本提示 → 视觉编码 → 时相交互 → 跨模态融合 → 变化解码 → 输出`。对每一步记录张量形状、训练阶段和证据页，例如 `[PDF p.4, Fig. 2]`。

## 5. 关键图表

| 图表 | 应怎样阅读 | 能支持什么 | 证据定位 |
|---|---|---|---|
| Fig. 2（占位） | 从输入沿箭头追踪融合位置 | 只支持结构描述 | `[PDF p.4, Fig. 2]` |
| Table 2（占位） | 先核对数据划分与指标 | 只支持该设定下的比较 | `[PDF p.7, Table 2]` |

## 6. 关键公式

对占位公式逐项给出符号表、张量形状、计算顺序、设计动机、小例子和假设；不根据常见做法补写论文未报告的细节。证据定位：`[PDF p.5, Eq. 3]`。

## 7. 实验与证据

按数据集、划分、指标、可比基线、主结果、消融、鲁棒性、效率和缺失证据组织。虚构数值不得写成真实结果；缺失处写 `论文未报告`。

## 8. 主张、证据与推断

| 作者主张 | 证据支持范围 | 分析/推断 |
|---|---|---|
| 文本引导改善变化推理（占位） | 仅限论文报告的数据与消融 `[PDF p.7]` | 对新传感器是否成立仍需验证 |

## 9. 局限与失败条件

区分 `作者明确说明 [PDF p.x]` 与 `基于证据的风险推断`；对没有实验支撑的失败场景保留不确定性。

## 10. 对研究台账的影响

- `论文索引`：新增 `P900`。
- `创新假设`：`I001` 从 `候选` 调整为 `被已有工作覆盖`，保留原始行并填写覆盖论文 `P900`。
- `模块方法库`：新增占位模块 `M900`，状态 `待评估`。
- `待读问题`：新增 `Q900`，要求在可比数据划分下核验覆盖关系。

## 11. 主动回忆

先列五个问题，再使用明显分隔线给出答案。问题覆盖任务定义、融合位置、损失、最关键证据和一个失败条件。

---

### 主动回忆答案

答案必须能回指前述章节和 PDF 页码，不引入新主张。

## 12. PDF 页码索引

| 主张/主题 | PDF 页 | 图表/公式 | 笔记章节 |
|---|---:|---|---|
| 方法总览（占位） | 4 | Fig. 2 | 4、5 |
| 覆盖 `I001` 的证据（占位） | 5、7 | Eq. 3、Table 2 | 6、7、10 |
